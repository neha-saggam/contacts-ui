import React from "react";
import ReactDOM from "react-dom";
import {Navbar} from 'react-materialize';

export function NavBar(props) {
  return   <Navbar brand='Contacts' right style={{backgroundColor: "#2a56c6"}}>
    </Navbar>;
}
